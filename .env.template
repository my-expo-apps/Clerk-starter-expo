# Copy to .env for local CLI validation only.
# Never commit real secrets.

# Supabase
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

# Optional: allow the CLI validator to attempt calling the Edge bootstrap.
# Must be a valid Clerk JWT for your project (issuer + audience match).
CLERK_TEST_JWT=